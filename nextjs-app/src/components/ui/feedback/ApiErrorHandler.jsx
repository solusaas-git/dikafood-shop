/**
 * ApiErrorHandler component - No longer needed with new API service
 * The new API service is simpler and doesn't require initialization
 * Keeping this component for backward compatibility but it does nothing
 */
const ApiErrorHandler = () => {
  // This component doesn't render anything and no longer needs to initialize anything
  return null;
};

export default ApiErrorHandler;